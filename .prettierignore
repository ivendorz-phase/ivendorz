# Frozen corpus + governance docs — never reformat (authoritative; not WP-0.11's concern).
generatedDocs/
governanceReviews/
*.md

# Quarantined standalone experiments — excluded from root build gates (REPOSITORY_STRUCTURE v1.1 P-4).
prototypes/

# Generated / build artifacts (gitignored).
generated-contracts-registry/
src/generated/
.next/
out/
build/
coverage/
playwright-report/
test-results/

# Lockfile (managed by pnpm).
pnpm-lock.yaml

# Local/editor + tooling files (not Wave-0 managed code).
.claude/
.husky/_/
.git.code-workspace
