# Dependencies
node_modules/

# Next.js build output
/.next/
/out/
/build/

# Generated artifacts (regenerated, never committed)
#  - Prisma codegen output lands here (Doc-6A §11.4: generated, gitignored, never hand-edited)
#  - the relocated Prisma client (src/generated/) is removed in WP-0.5; ignored defensively here
/generated-contracts-registry/
/src/generated/

# Environment / secrets (env vars only; never in code/commits/logs — CLAUDE.md §2)
.env
.env.*
!.env.example

# TypeScript incremental build info
*.tsbuildinfo

# Next.js generated TS shim (auto-regenerated by `next build`/`next dev`)
next-env.d.ts

# Test / coverage output
/coverage/
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/
/.playwright-mcp/

# Deploy
/.vercel/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor noise
.DS_Store
Thumbs.db
*.code-workspace

# Local-only design previews — throwaway visual references, never shipped (Review-A 2026-07-19).
# vendor-dashboard-preview reproduces a design canvas with sample data; it must not deploy as a
# public, unauthenticated route. Keep it local; rebuild the real surface from the kit with wired reads.
/app/vendor-dashboard-preview/

# Stray review/screenshot artifacts at repo root (never deliverables).
/referral-updated.png
/*-updated.png
/ds-0-overview.png
