// Doc-7B embedded tier (BR5) — single-owned, contract-bound, non-disclosure-bound presentation.
// Realized in this tranche: trust-badge (M5 Doc-5G public read; props-only until M5 lands).
//
// DEFERRED (demand-driven — bind each module's read when it exists, NOT a silent cap):
//   billing-indicator (M7 Doc-5I) · ai-advisory-panel (M9 Doc-5K) · conversation-thread (M6 Doc-5H).

export * from "./trust-badge";
