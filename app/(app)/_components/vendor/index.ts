// Vendor Workspace CONTENT components (presentation phase).
//
// The vendor's BESPOKE SHELL has been RETIRED — every authenticated workspace now composes the single
// canonical AppShell (`app/(app)/_components/shell`). Only content / loading / placeholder components
// remain here; the Vendor ShellViewModel lives in `./vendor-shell-vm`.
export { VendorContentSkeleton } from "./vendor-content-skeleton";
export {
  WorkspaceSectionPlaceholder,
  type WorkspaceSectionPlaceholderProps,
} from "./workspace-section-placeholder";
