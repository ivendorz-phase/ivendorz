# inngest declares peerOptional @sveltejs/kit, which transitively pulls vite@8 and clashes
# with vitest's vite@7 under npm's strict peer resolution. Svelte is unused — legacy-peer-deps
# ignores the unused optional peer so install/CI resolve cleanly.
legacy-peer-deps=true
